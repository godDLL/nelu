# Scratch: throwaway probes, build artefacts, captures. Never tracked.
/tmp/
/AGENT.md

# Top-level scratch probes from ad-hoc verification.
/div.nelua
/d1.nelua
/rec.nelua
/rec_bin

# Build artefacts.
/*.bin
/*.c

# Upstream Nelua launcher scripts (we ship our own under src/; these are the
# reference compiler's entry points and must not be published as ours).
/nelua
/nelua.bat
/nelua-lua
/nelua.lua

# Nim compile artefacts from `nim c -r --path:src src/<module>.nim` self-tests.
nim_cache/
*.o
/src/analyzer
/src/ast
/src/astshapes
/src/cemitter
/src/cgen
/src/cgen_types
/src/lexer
/src/parser
/src/preprocessor
/src/sema
/src/typedesc
/src/types
/src/main
# Consolidated design docs + survey probes (scratch, not tracked).
/plan/
