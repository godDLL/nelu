-- Nelu: `<raises>` lists the exceptions a function may propagate.  The
-- oracle rejects the annotation; ours accepts it and runs the body.
local function compute(x: integer): integer <raises>
  return x * 2
end
print(compute(7))
