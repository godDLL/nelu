-- void_return.nelua -- extracted from zxplayer nelua/player.nelua
-- (the mpv command dispatchers). A function declared `: void` that
-- returns with no value. Isolates: the `void` return type.
local function noop(): void
  print('in')
  return
end

noop()
print('after')