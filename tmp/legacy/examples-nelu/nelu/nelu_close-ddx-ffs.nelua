-- Nelu: `<close>` marks a function as running resource-cleanup.  The oracle
-- rejects the annotation; ours accepts it and runs the body.
local R = @record{ x: integer }
function f(): void <close>
  local r = R{ x = 1 }
end
f()
print("ok")
