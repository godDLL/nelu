-- Median of a small even-length array after sorting it.
local a: [4]integer = {3, 1, 4, 2}
for i = 1, #a - 1 do
  local j = i
  while j > 0 and a[j - 1] > a[j] do
    a[j - 1], a[j] = a[j], a[j - 1]
    j = j - 1
  end
end
local mid = #a // 2
local med = (a[mid - 1] + a[mid]) / 2.0
print(med)
